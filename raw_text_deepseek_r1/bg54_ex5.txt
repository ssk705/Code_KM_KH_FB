[CODE]  
interface Readable { void read(); }  
interface Writable { void write(); }  
class FileHandler implements Readable, Writable {  
    public void read() { /* Read logic */ }  
    // Missing write method  
}  
[OUTPUT]  
KM: The `FileHandler` class implements `Writable` but does not define the required `write` method, causing a compilation error.  
KH: Add a `public void write()` method in `FileHandler` to fulfill the `Writable` interface contract.