[CODE]
class Shape {
    public String name = "Shape";
    public void printType() {
        System.out.println(name);
    }
}

class Circle extends Shape {
    public String name = "Circle";
}

public class Main {
    public static void main(String[] args) {
        Shape s = new Circle();
        s.printType(); 
    }
} 
[OUTPUT]
KM:Fields aren't polymorphic. `printType()` uses `name` from `Shape` (compile-time type), not `Circle`, because field resolution is static.
KH:Access fields via getter methods instead of directly to leverage polymorphism.  
