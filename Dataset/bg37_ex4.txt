[CODE]
public class Shop {
    public static void main(String[] args) {
        int price = 100;
        {
            private int discount = 10;   
            price = price - discount;
        }
        System.out.println(price);
    }
}
[OUTPUT]
KM:Using `private` inside a local block (within `main()`) is invalid. Access modifiers only apply to class members (fields/methods).
KH:Declare `discount` without `private`. Use `int discount = 10;` instead.  
