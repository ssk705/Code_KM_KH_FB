[CODE]
class Fruit {}
class Apple extends Fruit {}

class Tree {
    public Fruit getFruit() {
        return new Fruit();
    }
}

class AppleTree extends Tree {
    @Override
    public Apple getFruit() {   
        return new Apple();
    }
}

class SpecialTree extends AppleTree {
    @Override
    public Fruit getFruit() {  
        return new Fruit();
    }
}
[OUTPUT]
KM: While covariant returns (subclassing return type) *are* allowed in overriding, reverting to a superclass return type (`Apple` → `Fruit`) violates the contract. 
KH: Return types must be *subtypes* of the original return type, not supertypes.  